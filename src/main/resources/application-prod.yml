spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:postgres}:${DB_PORT:5432}/${DB_NAME:bootcamp}
    username: ${DB_USER:bootcamp}
    password: ${DB_PASSWORD:bootcamp}
    driver-class-name: org.postgresql.Driver

  h2:
    console:
      enabled: false