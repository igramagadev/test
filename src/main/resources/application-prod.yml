spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:postgres}:${DB_PORT:5432}/${DB_NAME:mesupport}
    username: ${DB_USER:mesupport}
    password: ${DB_PASSWORD:mesupport}
    driver-class-name: org.postgresql.Driver
