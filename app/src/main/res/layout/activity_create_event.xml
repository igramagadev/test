<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/main_green">

    <ImageView
    android:id="@+id/btnCloseCreate"
    android:layout_width="56dp"
    android:layout_height="56dp"
    android:layout_marginStart="24dp"
    android:layout_marginTop="32dp"
    android:background="@drawable/bg_btn_close_white"
    android:src="@drawable/ic_close"
    app:tint="#439A6B" android:padding="12dp"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground"
    app:layout_constraintTop_toTopOf="parent"
    app:layout_constraintStart_toStartOf="parent" />

    <com.google.android.material.card.MaterialCardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="24dp"
        app:cardBackgroundColor="@color/event_white" app:cardCornerRadius="12dp"
        app:cardElevation="8dp"
        app:strokeWidth="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="20dp">

            <EditText
                android:id="@+id/etCreateTitle"
                android:layout_width="match_parent"
                android:layout_height="48dp"
                android:background="@drawable/bg_input_edit"
                android:hint="@string/event_name_admin"
                android:textColorHint="@color/text_grey"
                android:textColor="@color/black"
                android:textSize="14sp"
                android:paddingHorizontal="16dp"
                android:layout_marginBottom="16dp"
                android:inputType="text" />

            <EditText
                android:id="@+id/etCreateDescription"
                android:layout_width="match_parent"
                android:layout_height="180dp" android:background="@drawable/bg_input_edit"
                android:gravity="top|start" android:hint="@string/event_description_admin"
                android:textColorHint="@color/text_grey"
                android:textColor="@color/black"
                android:textSize="14sp"
                android:padding="16dp"
                android:layout_marginBottom="16dp"
                android:inputType="textMultiLine" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_marginBottom="24dp"
                android:baselineAligned="false">

                <EditText
                    android:id="@+id/etCreateDate"
                    android:layout_width="0dp"
                    android:layout_height="48dp"
                    android:layout_weight="1"
                    android:layout_marginEnd="8dp"
                    android:background="@drawable/bg_input_edit"
                    android:hint="@string/event_date_admin"
                    android:textColorHint="@color/text_grey"
                    android:textColor="@color/black"
                    android:textSize="14sp"
                    android:paddingHorizontal="16dp"
                    android:inputType="date" />

                <EditText
                    android:id="@+id/etCreateLocation"
                    android:layout_width="0dp"
                    android:layout_height="48dp"
                    android:layout_weight="1"
                    android:layout_marginStart="8dp"
                    android:background="@drawable/bg_input_edit"
                    android:hint="@string/event_adress_admin"
                    android:textColorHint="@color/text_grey"
                    android:textColor="@color/black"
                    android:textSize="14sp"
                    android:paddingHorizontal="16dp"
                    android:inputType="textPostalAddress" />
            </LinearLayout>

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnSubmitEvent"
                android:layout_width="match_parent"
                android:layout_height="60dp"
                android:textColor="#FFFFFF"
                android:text="@string/btn_create_event"
                android:textAllCaps="false"
                android:textStyle="bold"
                android:textSize="14sp"
                app:backgroundTint="@color/main_green"
                app:cornerRadius="6dp" />

        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

</androidx.constraintlayout.widget.ConstraintLayout>